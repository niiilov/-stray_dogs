// import { ThemeProvider } from "./ThemeProvider";
import { Router } from "@pages/index";

export const App = () => {
  return (
    // <ThemeProvider>
    <Router />
    // </ThemeProvider>
  );
};
